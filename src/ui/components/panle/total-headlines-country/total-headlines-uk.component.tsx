import { TotalCard } from '../../../../ui/components';

export const TotaUKHeadlines = ({ total }: { total: number }) => {
	return (
		<TotalCard
			title='Total the United
    Kingdom Headlines'
			total={total}
		/>
	);
};
